{
  "name": "@lorenstuff/mysql-dump-archive",
  "version": "2.0.2",
  "description": "A library for dumping MySQL databases and creating an archive of their contents.",
  "main": "./lib/index.js",
  "scripts": {
    "build": "npx rimraf lib && npx tsc",
    "build:watch": "npx rimraf lib && npx tsc --watch"
  },
  "author": "Loren Goodwin",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "@types/archiver": "^6.0.2",
    "@types/node": "^22.1.0",
    "archiver": "^7.0.1",
    "mysql2": "^3.11.0"
  },
  "devDependencies": {
    "@donutteam/typescript-config": "^1.0.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.5.4"
  }
}
